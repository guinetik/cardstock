---
id: 30
title: "Stamp new cards from a stencil instead of retyping the checklist"
summary: "Every integration card gets the same five-step checklist retyped by hand, once per integration, several per client. A stencil defines the kind of work once - title, summary, checklist, tags - and stamping a new card fills it all in."
status: built
epic: "Board & cards"
area: "UI"
tags:
  - enhancement
  - card
lane: building
effort: H
assignee: joao@staffeto.com
planned_start: 2026-09-10
target: 2026-09-10
---
# #30 — Stamp new cards from a stencil instead of retyping the checklist

## Ask

From the 2026-09-09 call. Hap, on what a delivery card contains: "On the subtasks, I believe for delivery, it's always going to be the same five steps. If we find that there's a sixth, we can add a sixth." And the shape of Sanjay's week: every new client means one card per integration, each given the same checklist by hand — credentialing, field mapping, development, testing, sign-off, go-live. The list never changes; he retypes it once per integration, several integrations per client.

The checklist is a property of the kind of work, not of the card. Nothing in the product says so, so it gets re-entered every time — and two cards at 3/5 mean nothing comparable until the lists are the same list.

## Status

Built 2026-09-10. Board-scoped stencils, the configuration editor, and the lane add-card chooser are implemented. Stamping copies editable fields and creates real checklist rows; later stencil changes leave existing cards alone. Documentation: `docs/stencils.md`.

Verification: 21 focused stencil/core checklist unit tests, all 6 stencil browser cases, and the transactional database/RLS test pass. TypeScript, package checks, changed-file Biome checks, and `git diff --check` pass. The full unit suite before the import follow-up reported 560 pass, 27 skip, and 3 failures in unchanged lane-map and issue-body-editor contract tests (missing app-router context and an outdated source-string assertion). No production build was run.

Recovery fixes made the browser cases independent, accepted Windows Markdown line endings, selected priority by its combobox role, and scoped the duplicate-name alert to the stencil section.

Template import: hand-written checkbox lines without list bullets import with corrected formatting and an amber warning with an icon and review prompt. Invalid checklist content shows a red inline error without losing the draft. The saved board template is unchanged.

## Checklist

- [x] Add board-scoped card_stencils and card_stencil_tags tables, unique names, effort constraints, timestamps, and cascade behavior.
- [x] Add membership RLS and database tests for access isolation, invalid rows, unique names, and tag deletion cascades.
- [x] Implement stencil types, input validation, checklist serialization, and conversion to card initial values in src/lib/stencils.ts.
- [x] Add create, update, and delete server actions with canManage checks and project/board binding validation.
- [x] Load stencils and their tags with board data, sorted alphabetically by name.
- [x] Add the stencils section below card template on board manage, with step/tag counts, add/edit/delete controls, and permission messaging.
- [x] Build the stencil editor with editable name, title, summary, body, area, effort, and tags using the card dialog layout.
- [x] Add an option to start a new stencil from the board's existing card template without changing that template.
- [x] Provide checklist item editing and reordering with always-unchecked items, preserving unrelated body content through parseChecklist/writeChecklist.
- [x] Add the lane add-card chooser with Blank card and available stencils; boards without stencils open the existing blank dialog directly.
- [x] Stamp editable card initial values through the existing create flow, preserving lane context and leaving per-card epic, priority, assignee, and dates uninherited.
- [x] Verify stamping creates real card_checklist_items and that later stencil edits or deletion leave existing cards unchanged.
- [x] Add authoring end-to-end coverage for a three-step stencil, saved Markdown, and the displayed step count; unit-test stencil validation.
- [x] Add stamping end-to-end coverage for persisted checklist rows, stencil deletion, tags, and the unchanged blank-card flow.

## Rationale

Stencils are what turn the checklist from a per-card note into data worth aggregating: when every delivery card is stamped from one stencil, 3/5 means the same pipeline position everywhere and can roll up per client. Packets (a set of stencils stamped together) are phase 2 and out of scope here.
