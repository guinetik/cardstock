---
id: 18
title: Sync Markdown and board edits without losing either side
summary: Let local issue edits and board changes meet without relying on a fragile import/export order.
status: done
epic: Data & round-trip
area: Data
raised_by: Joao
raised: 2026-09-06
relates: [16, 17, 19]
tags:
  - enhancement
  - import-export
lane: done
priority: 1
rank: 8
---
# #18 — Sync Markdown and board edits without losing either side

## Ask

Apply a conflict-aware bidirectional sync plan with safe recovery.

## Status

Implemented in 516e026 and deployed on 2026-09-06 after Joao applied the database migration. Hosted CLI apply passed: #18/#19 updates uploaded, board priorities and ordering downloaded, explicit conflicts resolved, and the final plan returned clean with zero pending changes. Sync applies the three-way plan in both directions, with explicit ours/theirs conflict choices, transactional remote batches, immutable identity/revision checks, idempotent retries, and resumable per-card local/baseline checkpoints. Ours means local Markdown; theirs means the hosted board. Legacy app-owned/file-owned body flags never choose the winner.

## Rationale

Let local issue edits and board changes meet without relying on a fragile import/export order.

## Evidence

Audit on 2026-09-06 found client guidance says import then export with protected priority, while current ETL takes priority from files and recommends export then import. Neither ordering alone protects concurrent edits.

The reusable planner and lossless materializer live in packages/core/src. The executor, no-clobber file publication and recovery journal live in packages/cli/src/sync-*.ts. The protocol-2 snapshot/apply routes use supabase/migrations/20260914000000_cli_sync_apply.sql for coherent reads, revision-checked transactional card/tag/link/event writes and durable retry receipts. Revisions also cover derived fields such as rank changes caused by neighboring cards.

Verification on 2026-09-06: 57 core tests, 8 journal tests, 31 Node CLI integration tests and 11 isolated local-Supabase integration tests pass (107 total). Coverage includes both directions, optional-field removal, unknown nested YAML, same-field conflicts, concurrent identity creation, stale revisions, full transaction rollback, permissions, lost responses, forced process termination after remote commit and local checkpoint, no duplicate retries, retained originals and clean second previews. The built Node CLI also round-trips through the actual authenticated routes and local database. All 12 apply/recovery tests also pass against an isolated npm tarball installation. Package/app TypeScript, scoped lint and all 21 tracker items pass validation.

## Residuals

Hosted smoke on 2026-09-06 exercised a real recovery boundary: moving #18 out of unsorted changed #21's derived rank after the remote batch committed. Verification stopped without overwriting local files. The recorded intent was archived with --abort (no rollback), then a fresh plan explicitly accepted #21's current board priority/rank and completed clean. Before-images and the interrupted/completed journals were retained. A possible usability follow-up is automatic re-planning of derived neighbor-rank changes; the current path deliberately requires reconciliation.

Old baselines require agreement-based refresh or explicit --adopt-identities; known UUID mismatches cannot be overridden. Resume and abort preserve recovery artifacts; abort does not roll back committed writes.

Closed: this CLI is a published npm package as of [[14]]'s update, so "use the
repository CLI until release" no longer applies. Cross-client mapping parity and
retirement of old sync shims shipped as [[19]]. Unsupported mapping
overrides/custom audience rules are still refused rather than silently ignored,
by design, not as an open gap. Plan: docs/plans/2026-09-06-cli-sync-apply.md.
Rollout: docs/deploy.md. Recovery: packages/cli/README.md.
