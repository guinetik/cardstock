---
id: 17
title: Preview local and board changes before syncing
summary: See what would go up, come down or conflict before either side changes.
status: built
epic: Data & round-trip
area: Data
raised_by: Joao
raised: 2026-09-06
relates: [14, 15, 16, 18]
tags:
  - enhancement
  - import-export
lane: building
rank: 3
priority: 2
effort: M
planned_start: 2026-09-06
target: 2026-09-06
---
# #17 — Preview local and board changes before syncing

## Ask

Add status and sync --dry-run with an exact plan and readable or JSON output.

## Status

Built and verified against the hosted board on 2026-09-06. Implemented in 207178b: status and sync --dry-run read authenticated snapshots and compare local, baseline and remote values per field. They report uploads, downloads, equal changes and conflicts without writing files or board data. The explicit baseline command saves only agreed, valid state per checkout. The signed-in smoke test passed, including clean recovery after removing the temporary edits.

## Rationale

See what would go up, come down or conflict before either side changes.

## Evidence

The current wrapper estimates uploads using file hashes and checks export output separately; it does not calculate one bidirectional plan.

116 focused tests pass, including the planner's conflict, identity, missing-card and unknown-field cases. All 17 Node CLI integration tests also pass against an isolated npm tarball installation. They verify GET-only previews, unchanged tracker/baseline files, atomic baseline persistence, authentication errors and changing snapshots. Package type checks, app TypeScript and scoped lint pass. Plan: docs/plans/2026-09-06-cli-sync-preview.md.

Hosted smoke test with Joao on cardstock.guinetik.com, project cardstock / board cardstock-dev: browser login succeeded; status and sync --dry-run returned clean with no diagnostics; baseline saved agreed state; a local #17 body edit produced one upload and zero conflicts; a web-only body edit produced one download and zero conflicts; different body edits on both sides produced one conflict and ok: false. Removing both temporary edits restored clean: true with all counts zero. These tests used the repository CLI; dry-run did not apply changes to either side.

## Residuals

Applying changes and verified baseline advancement remain #18; API mapping overrides and client cutovers remain #19. The current API provides external IDs, so immutable identity for deleted/recreated cards is still a sync-integration consideration. The hosted smoke test returned no tracker validation diagnostics, including for the corrected #20 vocabulary. Repository CLI verification does not establish a published-package release.
