---
id: 19
title: Replace repository sync scripts with one CLI configuration
summary: Use the same installed CLI for Cardstock, Designer and Website without copying scripts or relying on local checkout paths.
status: wip
epic: Data & round-trip
area: Data
assignee: joao@staffeto.com
raised_by: Joao
raised: 2026-09-06
relates: [14, 17, 18]
tags:
  - enhancement
  - import-export
lane: now
rank: 1
priority: 1
effort: H
planned_start: 2026-09-06
target: 2026-09-06
---
# #19 — Replace repository sync scripts with one CLI configuration

## Ask

Migrate all three current tracker clients to the standalone CLI once its sync is ready.

## Status

The configuration and offline-validation slice is committed in 383abad. The CLI imports legacy board configuration with a preview and explicit diagnostics, preserves schemes and mappings, and validates board workflow and vocabulary rules. Cardstock's own CLI configuration is migrated. #17 adds read-only sync previews and agreed baselines in 207178b and has passed hosted smoke testing. #18 now implements the live bidirectional executor, transactional/revision-and-identity-checked API, explicit ours/theirs choices and interrupted-run recovery in 516e026. Its migration and server are deployed; hosted apply passed on 2026-09-06, including uploads, downloads, explicit conflict choices, safe recovery after a derived-rank change and a clean final plan. This migration remains WIP: existing sync shims cannot be retired until all three clients' round-trip parity is verified.

## Generic-field implementation

Direction clarified with Joao on 2026-09-06: use Cardstock's generic contract rather than reproduce Designer validation or Staffeto naming conventions. Area is free-form text. Epics are optional board entities, reused/created by name and clearable without deleting the epic. Audience is an independent all/internal filter field, not an access-control boundary and not inferred from any tag, area or epic name. A board need not have an internal tag.

Implemented locally: relaxed area/epic scheme validation, explicit audience frontmatter and import/export/sync, optional epic assignment, preservation of existing stored audience, and UI copy explaining classification. Protocol 3 and migration 20260915000000_explicit_card_audience.sql protect the new field semantics from older clients/servers. Legacy audience rules remain inert metadata; nonempty tag-derivation overrides are still refused. These new changes are not yet deployed or published.

Verification: 244 tests pass (190 core/frontmatter/import/filter checks, 8 journal checks, 32 Node CLI tests and 14 isolated local-database API tests); two older importer database tests were skipped in the non-database unit run. The real Node CLI verifies website audience/area downloads, explicit audience uploads and a clean final preview. Legacy internal classifications survive projection upgrades; no internal tag is required. New epic names are reused, and clearing an assignment keeps the epic. TypeScript, scoped lint and all 21 tracker files pass.

## Rationale

Use the same installed CLI for Cardstock, Designer and Website without copying scripts or relying on local checkout paths.

## Evidence

Clients: backlog/board.json; Staffeto delivery/designer/board.json and delivery/website/board.json. Differences include workflow rules, tag aliases, audience mappings and seeds.

At the configuration slice, 104 focused tests and eight Node CLI integration tests passed, including all three client fixtures, and all 19 then-current Cardstock items passed both validators. With #17, 116 focused tests and 17 Node CLI integration tests pass, including an isolated npm tarball installation. On 2026-09-06, #17 passed the signed-in hosted-board smoke test: login, baseline creation, upload/download detection, conflicts and clean recovery. The hosted preview returned no validation diagnostics after #20's vocabulary was corrected. Package checks, app TypeScript and scoped lint pass; the full repository check stops on existing lint errors including design-skill files. Implementation plan: docs/plans/2026-09-06-cli-tracker-migration.md.

## Residuals

Deploy the explicit-audience migration and protocol-3 application, then smoke-test generic area/epic/audience round trips. Publish the CLI, cut over Designer and Website configurations/runbooks, and retire legacy shims after the generic Cardstock contract passes. Designer-specific validation parity is not required. Existing aliases remain supported; legacy derived-tag mappings need explicit tags or a separately agreed generic mapping design, not hidden Staffeto rules. Repository CLI verification does not establish a published-package release. No need to add non-Cardstock providers in this migration.
