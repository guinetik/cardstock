Use `Eyebrow` for the mono-caps kicker above a title — the quietest label in the system.

```jsx
<Eyebrow>Demo</Eyebrow>
<h1>Product backlog</h1>
```

It is grey-faint, 10px, 0.11em tracking. Do not use it for status (that is `Stat`) or for a form-row label (that is `FieldLabel`).
