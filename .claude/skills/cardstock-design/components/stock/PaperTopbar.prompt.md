The app bar: card stock with a ruled bottom edge, 48px tall.

```jsx
<PaperTopbar right={<UserSlot />} />
```

cardstock has **no logo mark**. The wordmark is the product name in Newsreader, lowercase, 15px/600 — that is the brand signature, and nothing should be drawn in its place.
